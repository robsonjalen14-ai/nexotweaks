services:
  - type: web
    name: nxo-tweaks
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server/index.js
    envVars:
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: NODE_ENV
        value: production
